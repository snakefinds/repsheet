"""Double-click to add picks.ly to scraped JSON (no PowerShell). Requires Python 3."""
from gsheet_scraper.desktop_app_picksly_json import main

if __name__ == "__main__":
    raise SystemExit(main())
